name: "Check AWS CDK Update"

on:
  push:
    branches:
      - main

jobs:
  check-aws-cdk-update:
    runs-on: ubuntu-latest
    steps:
      - uses: morning-code/aws-cdk-update-checker@v0.0.12
      #- uses: morning-code/aws-cdk-update-pr-builder-action-ts@v1
